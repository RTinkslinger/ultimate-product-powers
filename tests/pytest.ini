[pytest]
markers =
    quick: L1 content snapshots + hook smoke tests (<1 min, gating)
    full: L2 trigger tests (xfail, informational) + L3 behavior tests (~60 min)
timeout = 300
timeout_method = thread
testpaths = .
